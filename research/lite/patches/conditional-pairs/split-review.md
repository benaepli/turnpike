# Iteration 67 split-evidence decision review

A second chunk is justified: the fixed-grid depth-8 gain could change the decision. A merge is not justified by this chunk alone. The research-loop-lite skill explicitly lets autonomous mode decide split evidence and depart from grader advice with a written reason. A replicated useful search improvement can therefore support an autonomous override of a failed mechanism prediction, but the log must still record that prediction as failed. This is decision discretion, not permission to revise the frozen hypothesis or claim its explanation was confirmed.

The decisive adverse finding is broader than the two failed observables: the fixed-grid improvement currently coincides with severe AOS harm. The retained primary population must stay fixed; excluding AOS from that primary does not make its harm irrelevant to the goal.

## What the existing evidence says

- The official fixed-grid depth>=8/second ratio is 1.1128. Overall throughput is 1.0443; those populations differ, so dividing these numbers does not yield the exact primary per-run ratio. The four grid arms have 8,785/519,480 candidate events/runs versus 7,897/501,000 baseline: per-run ratio 1.0729. Holding baseline grid-arm run weights fixed gives 1.0745. Thus the grid gain is not explained by throughput or changing weights among these four arms alone; its point remains below the frozen 1.08 lower edge, without establishing refutation from one chunk.
- AOS falls from 1,654/105,180 to 978/113,940 depth-8 events/runs: rate ratio about 0.546. Full-campaign depth-8/second rises only 2.15%, inside the recorded layout floor; full-campaign per-run ratio is about 0.9782. Baseline-weighted campaign per-run ratio is 0.9829. This is an important guard against calling a selective primary gain general search progress.
- Firing and evidence supply pass: 525,757 chosen runs, 110 qualified cells, 1,477,839 supported axis decisions. The supported final-argmax change share is 39,519/1,477,839 = 2.674%, far below 15%. B's own reward ratio is 0.9268, below 0.95; A and C are about 0.9978 and 1.0260. These failures stand regardless of the grader's merge advice.
- Source confirms that conditional categorical weights can change without changing their argmax. The existing total_variation_micro sums normalized probability-vector TV once per conditional axis decision. Dividing by the sum of cell decision-row decisions, 2,118,004, gives mean TV 0.03063. conditional_direction_draws counts warmed candidate directions, so it is the wrong TV denominator. This supports a small probability-shift explanation as an alternative, not a replacement pass criterion. There is no supported-only TV counter; do not label this mean supported-only.
- Fresh non-slot depth>=11 is 28/389,965 candidate versus 18/371,390 baseline: count ratio 1.556 and rate ratio about 1.481, exploratory and sparse. All-run violations remain zero in 633,420 candidate runs. Neither fact overrides primary or panel harm.

## Fixed manual checks for subsequent chunks

1. Keep the declared seeds, independent session resets, binary/configuration identities and frozen four-grid-arm primary unchanged. Report paired depth-8 per-run rates and intervals separately from per-second and throughput. Report each seed and each arm; do not retain only positive seeds. Continue within the existing four-chunk cap only while another chunk could change the decision.
2. Repeat the baseline-arm-weighted per-run calculation as a descriptive composition check, alongside the official primary. Report AOS and full-campaign rates separately on every seed. Replicated AOS harm of this size would make an override difficult to defend even if the grid primary passes; a 2% campaign throughput-assisted gain does not resolve it.
3. Use existing variant rows for fresh non-slot, replay-slot, probe and actual learner-tag strata: gradedRuns, depthAtLeast, stepsUsedSum, wallUsSum and planCompleteRuns exist. Report depth-8 rates, mean steps, wall per step, plan completion and exposure shares. Learner/coin membership is endogenous; these are descriptive strata, not randomized treatment controls. Do not match away changed ArmSet choices as if they were nuisance imbalance: changing that distribution is the intervention. Existing per-arm coin counters and raw per-cell conditional rows can locate concentration and AOS differences, but current aggregate rows cannot isolate cap maturation within a session.
4. Recompute the frozen support and argmax gates using decision-time counters, not final pair masses. Keep the original A/B/C reward guards and fresh non-slot depth-11 count/rate report. TV by cell/learner/parent-axis can describe probability movement, but cannot prove that movement caused extra deep runs; these counters are not joined to per-run depth.
5. Before any merge, run the unchanged regression suite and candidate protocol panel, comparing whole-binary rates on corresponding members against the existing baseline evidence. No selector tag identifies randomized conditional-model treatment. Check the epoch throughput ledger and source diff. Passing these checks would permit a documented decision override; it would not rescue the failed interaction explanation.

Recommendation: replicate, with special attention to AOS and the exact per-run primary. If robust primary and broad search utility remain favorable after the panel/regression checks, an autonomous merge can honestly be recorded as empirical utility despite a refuted argmax/reward-preservation story. If the gain stays concentrated in the grid while AOS or the panel loses materially, close and preserve the patch. Do not weaken gates, redefine the primary, or buy measurement-only experiments to rescue this candidate.

Sources inspected: the admitted frozen prediction; .agents/skills/research-loop-lite/SKILL.md; finish.json; support-1000.json; normal-1000.utilization.json; spur.patch; research/lite/state/conditional-pairs/chunk-1000.cand.json; paired baseline cache 34b5ef40e3bf-30-f9daa01b-300-g870d4c11-s3d907a50.json; current run_variant.rs. Calculations above only aggregate existing records. No exploration experiments or source edits.
