diff --git a/spur-core/src/simulator/arm_selector.rs b/spur-core/src/simulator/arm_selector.rs
index c90b227..f26441a 100644
--- a/spur-core/src/simulator/arm_selector.rs
+++ b/spur-core/src/simulator/arm_selector.rs
@@ -28,12 +28,13 @@
 //! coin-drawn.
 //!
 //! Each learner has a reward of its own, a per-run bool the scheduler sets
-//! when the run reached the shape the learner is after. A learner observes
-//! every coin-drawn run, whichever learner drew it, and its own learner
-//! runs, credited to the five directions the run carried, and never another
-//! learner's runs, so the three learners are read against one common
-//! control. Further rewards are read on the coin-drawn runs for calibration
-//! and steer nothing.
+//! when the run reached the shape the learner is after. Every learner
+//! observes every run that is not a probe, coin-drawn or drawn by any
+//! learner, credited with its own reward to the five directions the run
+//! carried, so each learner keeps its own reward and its own table while
+//! the three share the runs. The coin-drawn runs remain the common control:
+//! the reward rates per direction and combination, and the further rewards
+//! read for calibration, are read on them only and steer nothing.
 //!
 //! The selector's categorical draws come from a generator seeded from the
 //! run's schedule seed under a salt of its own, so the run's schedule
@@ -425,7 +426,9 @@ pub fn choose(run_id: i64, schedule_seed: u64, arm_index: i32, cell: Cell) -> Ch
     }
 }
 
-fn credit(learner: Learner, cell: Cell, arms: &ArmSet, reward: bool) {
+/// Credit one run to `learner`; `cross` says a learner run is credited to
+/// a learner other than the one that drew it.
+fn credit(learner: Learner, cell: Cell, arms: &ArmSet, reward: bool, cross: bool) {
     let pooled = is_pooled(cell);
     CELLS[learner.index()]
         .entry(cell)
@@ -434,15 +437,14 @@ fn credit(learner: Learner, cell: Cell, arms: &ArmSet, reward: bool) {
             CellLearner::new()
         })
         .credit(arms, reward);
-    if pooled {
-        util_stats::record_arm_selector_pooled_credit(learner.index());
-    }
+    util_stats::record_arm_selector_credit(learner.reward(), learner.index(), pooled, cross);
 }
 
-/// Fold one finished run into the learners that may see it: a coin-drawn
-/// run into every learner, a learner run into its learner only, a probe
-/// into none. The coin-drawn runs are also where every reward's rate per
-/// direction, and the step of the first request-caused entry, are read.
+/// Fold one finished run into every learner, each with its own reward,
+/// whether the run was coin-drawn or drawn by one learner; a probe is
+/// folded into none. The coin-drawn runs alone are where every reward's
+/// rate per direction, and the step of the first request-caused entry,
+/// are read; a learner run is read for its drawing learner's reward only.
 /// `arm_index` is the campaign arm the run ran under, for the per-arm
 /// counters; `cell` is where the credit lands.
 pub fn observe(arm_index: i32, cell: Cell, run_id: i64, choice: &Choice, rewards: &Rewards) {
@@ -463,7 +465,7 @@ pub fn observe(arm_index: i32, cell: Cell, run_id: i64, choice: &Choice, rewards
     match choice.learner {
         None => {
             for learner in Learner::ALL {
-                credit(learner, cell, arms, rewards_of(learner, rewards));
+                credit(learner, cell, arms, rewards_of(learner, rewards), false);
             }
             for reward in Reward::ALL {
                 if let Some(r) = value(reward) {
@@ -474,10 +476,12 @@ pub fn observe(arm_index: i32, cell: Cell, run_id: i64, choice: &Choice, rewards
                 util_stats::record_client_anchor_first_post_fault_entry(arms, step);
             }
         }
-        Some(learner) => {
-            let r = rewards_of(learner, rewards);
-            credit(learner, cell, arms, r);
-            util_stats::record_arm_selector_observation(learner.reward(), true, arm_index, arms, r);
+        Some(drawer) => {
+            for learner in Learner::ALL {
+                credit(learner, cell, arms, rewards_of(learner, rewards), learner != drawer);
+            }
+            let r = rewards_of(drawer, rewards);
+            util_stats::record_arm_selector_observation(drawer.reward(), true, arm_index, arms, r);
         }
     }
 }
@@ -774,42 +778,76 @@ mod tests {
     }
 
     #[test]
-    fn each_learner_sees_the_coin_drawn_runs_and_its_own_runs_only() {
+    fn every_learner_sees_every_run_with_its_own_reward() {
         let _serial = config_override::exclusive_session();
         fault_timing::reset();
+        util_stats::set_enabled(true);
         reset();
         let cell = (4, 4);
         let arms = ArmSet::default();
+        let d = arms.directions()[0];
         let observations =
             |l: Learner| CELLS[l.index()].get(&cell).map_or(0, |state| state.observations);
-        // A learner's runs reach its own cell and no other's.
+        let alpha = |l: Learner| CELLS[l.index()].get(&cell).map_or(0.0, |state| state.alpha[d]);
+        let before = util_stats::snapshot().arm_selector_axis;
+        // A learner's runs reach every learner's cell, each learner with
+        // its own reward: B's runs below reward A only.
         for l in Learner::ALL {
             let id = assigned_id(l);
+            let rewards = rewarded(l == Learner::AbsorberCycle, false);
             for _ in 0..WARMUP_OBSERVATIONS {
-                observe(cell.0, cell, id, &learned(arms, l), &rewarded(true, true));
+                observe(cell.0, cell, id, &learned(arms, l), &rewards);
             }
             for other in Learner::ALL {
-                let want = if other.index() <= l.index() { WARMUP_OBSERVATIONS } else { 0 };
+                let want = WARMUP_OBSERVATIONS * (l.index() as u64 + 1);
                 assert_eq!(observations(other), want, "{other:?} after {l:?}'s runs");
             }
         }
-        // A coin-drawn run reaches every learner, whichever third drew it.
+        assert!(alpha(Learner::OvertakenGhost) > 0.0);
+        assert_eq!(alpha(Learner::AbsorberCycle), 0.0);
+        assert_eq!(alpha(Learner::CycleBeforeRequest), 0.0);
+        let sections = |s: &util_stats::ArmSelectorAxisStats| {
+            [s.overtaken_ghost.clone(), s.absorber_cycle.clone(), s.cycle_before_request.clone()]
+        };
+        let after = util_stats::snapshot().arm_selector_axis;
+        let learner_runs = 3 * WARMUP_OBSERVATIONS;
+        assert_eq!(after.cross_credited_runs - before.cross_credited_runs, 2 * learner_runs);
+        let then = sections(&before);
+        for (l, (now, then)) in Learner::ALL.iter().zip(sections(&after).iter().zip(then.iter())) {
+            assert_eq!(now.observations - then.observations, learner_runs, "{l:?}: credits");
+            // A learner run is read for its drawing learner's reward only,
+            // and the calibration tables read nothing from it.
+            let own = now.reward_runs_treated - then.reward_runs_treated;
+            assert_eq!(own, WARMUP_OBSERVATIONS, "{l:?}: own runs");
+            assert_eq!(now.reward_runs_control, then.reward_runs_control, "{l:?}: control runs");
+            assert_eq!(now.control_runs_by_direction, then.control_runs_by_direction, "{l:?}");
+            assert_eq!(now.control_runs_by_combination, then.control_runs_by_combination, "{l:?}");
+        }
+        assert_eq!(after.either_shape.reward_runs_control, before.either_shape.reward_runs_control);
+        assert_eq!(after.ghost_signal.reward_runs_control, before.ghost_signal.reward_runs_control);
+        // A coin-drawn run reaches every learner, whichever third drew it,
+        // and rewards A and neither B nor C.
         observe(cell.0, cell, assigned_id(Learner::AbsorberCycle), &coin(arms), &rewarded(true, false));
         for l in Learner::ALL {
-            assert_eq!(observations(l), WARMUP_OBSERVATIONS + 1, "{l:?} after a coin-drawn run");
+            assert_eq!(observations(l), learner_runs + 1, "{l:?} after a coin-drawn run");
         }
-        // Each learner is credited with its own reward: the coin-drawn run
-        // above rewarded A and neither B nor C.
-        let d = arms.directions()[0];
-        let alpha = |l: Learner| CELLS[l.index()].get(&cell).unwrap().alpha[d];
         assert!(alpha(Learner::OvertakenGhost) > alpha(Learner::AbsorberCycle));
         assert!(alpha(Learner::OvertakenGhost) > alpha(Learner::CycleBeforeRequest));
+        let after = util_stats::snapshot().arm_selector_axis;
+        assert_eq!(after.cross_credited_runs - before.cross_credited_runs, 2 * learner_runs);
+        for (l, (now, then)) in Learner::ALL.iter().zip(sections(&after).iter().zip(then.iter())) {
+            assert_eq!(now.observations - then.observations, learner_runs + 1, "{l:?}: credits");
+            assert_eq!(now.reward_runs_control - then.reward_runs_control, 1, "{l:?}: control");
+        }
+        let es = after.either_shape.reward_runs_control - before.either_shape.reward_runs_control;
+        assert_eq!(es, 1);
         // A probe is observed by none.
         let probe = id_where(run_cap::is_probe);
         observe(cell.0, cell, probe, &coin(arms), &rewarded(true, true));
         for l in Learner::ALL {
-            assert_eq!(observations(l), WARMUP_OBSERVATIONS + 1, "{l:?} after a probe");
+            assert_eq!(observations(l), learner_runs + 1, "{l:?} after a probe");
         }
+        util_stats::set_enabled(false);
         reset();
     }
 
@@ -1259,6 +1297,15 @@ mod tests {
         assert_eq!(er.reward_runs_control - er0.reward_runs_control, 3);
         assert_eq!(er.reward_positive_control - er0.reward_positive_control, 1);
         assert_eq!(after.observations - before.observations, 8);
+        // Five learner runs, each credited to the two learners that did
+        // not draw it; every learner is credited with all eight runs.
+        assert_eq!(after.cross_credited_runs - before.cross_credited_runs, 10);
+        assert_eq!(after.overtaken_ghost.observations - before.overtaken_ghost.observations, 8);
+        assert_eq!(after.absorber_cycle.observations - before.absorber_cycle.observations, 8);
+        assert_eq!(
+            after.cycle_before_request.observations - before.cycle_before_request.observations,
+            8
+        );
         // Learner A at the top level and under its reward name.
         assert_eq!(after.reward_runs_control - before.reward_runs_control, 3);
         assert_eq!(after.reward_positive_control - before.reward_positive_control, 1);
diff --git a/spur-core/src/simulator/util_stats.rs b/spur-core/src/simulator/util_stats.rs
index deeba8c..20985fb 100644
--- a/spur-core/src/simulator/util_stats.rs
+++ b/spur-core/src/simulator/util_stats.rs
@@ -209,9 +209,12 @@ impl Reward {
 /// The arm selector's counters for one reward: the coin-drawn runs and
 /// rewards per direction and per combination, the reward on the coin-drawn
 /// runs and on the learner runs, and, for a reward a learner is trained
-/// on, that learner's runs.
+/// on, that learner's runs and the credits into its cells.
 struct AxCounters {
     chosen_runs: AtomicU64,
+    /// Runs credited into the cells of this reward's learner, whichever
+    /// learner drew them.
+    credits: AtomicU64,
     departures: AtomicU64,
     axis_leader_agreements: AtomicU64,
     axis_draws: AtomicU64,
@@ -242,6 +245,7 @@ impl AxCounters {
     const fn new() -> Self {
         Self {
             chosen_runs: AtomicU64::new(0),
+            credits: AtomicU64::new(0),
             departures: AtomicU64::new(0),
             axis_leader_agreements: AtomicU64::new(0),
             axis_draws: AtomicU64::new(0),
@@ -268,6 +272,8 @@ impl AxCounters {
 
 static AX: [AxCounters; 7] = [const { AxCounters::new() }; 7];
 static AX_OBSERVATIONS: AtomicU64 = AtomicU64::new(0);
+/// Credits of a learner run into a learner other than the one that drew it.
+static AX_CROSS_CREDITS: AtomicU64 = AtomicU64::new(0);
 /// The first learner's reward per campaign arm over every observed run,
 /// slot `arm_index + 1` so an unattributed run lands in slot zero; arms
 /// past the width share the last slot.
@@ -2372,13 +2378,22 @@ pub fn record_arm_selector_cell_created(reward: Reward, pooled: bool) {
     }
 }
 
-/// A run was credited into a cell shared across campaign arms by the
-/// learner at `learner` (its position among the three).
-pub fn record_arm_selector_pooled_credit(learner: usize) {
+/// A run was credited into a cell of the learner trained on `reward`, at
+/// position `learner` among the three; `pooled` says the cell is shared
+/// across campaign arms and `cross` that a learner run was credited into a
+/// learner other than the one that drew it.
+pub fn record_arm_selector_credit(reward: Reward, learner: usize, pooled: bool, cross: bool) {
     if !enabled() {
         return;
     }
-    AX_POOLED.observations_by_learner[learner.min(AX_LEARNERS - 1)].fetch_add(1, Ordering::Relaxed);
+    AX[reward.index()].credits.fetch_add(1, Ordering::Relaxed);
+    if pooled {
+        AX_POOLED.observations_by_learner[learner.min(AX_LEARNERS - 1)]
+            .fetch_add(1, Ordering::Relaxed);
+    }
+    if cross {
+        AX_CROSS_CREDITS.fetch_add(1, Ordering::Relaxed);
+    }
 }
 
 /// The learners were cleared, so their gauges read empty.
@@ -4673,9 +4688,11 @@ impl ArmSelectorRewardStats {
 /// its warmup; `axis_leader_agreements / axis_draws` sits well above the
 /// share independent draws would give when the learner ran as intended.
 /// `departures` counts learner runs whose set differs from the id's coins;
-/// `cells` is a gauge of the learner's table. `reward_runs_treated` and
-/// `reward_positive_treated` are the learner runs; the control fields are
-/// the coin-drawn runs, and `chosen_by_*` what the learner runs took.
+/// `cells` is a gauge of the learner's table and `observations` the runs
+/// credited into it, whichever learner drew them, so `observations /
+/// cells` is the mean observations per cell. `reward_runs_treated` and
+/// `reward_positive_treated` are the learner's own runs; the control fields
+/// are the coin-drawn runs, and `chosen_by_*` what the learner runs took.
 /// `leader_margin_micro` is, per axis, the sum in millionths over the
 /// learner runs of the pairwise probability that the axis's highest-mean
 /// direction leads the next; divided by `chosen_runs` it is the mean
@@ -4684,6 +4701,7 @@ impl ArmSelectorRewardStats {
 #[derive(Serialize, Debug, Clone)]
 pub struct ArmSelectorLearnerStats {
     pub chosen_runs: u64,
+    pub observations: u64,
     pub departures: u64,
     pub axis_leader_agreements: u64,
     pub axis_draws: u64,
@@ -4710,6 +4728,7 @@ impl ArmSelectorLearnerStats {
         let load = |a: &[AtomicU64]| a.iter().map(|n| n.load(Ordering::Relaxed)).collect();
         Self {
             chosen_runs: ax.chosen_runs.load(Ordering::Relaxed),
+            observations: ax.credits.load(Ordering::Relaxed),
             departures: ax.departures.load(Ordering::Relaxed),
             axis_leader_agreements: ax.axis_leader_agreements.load(Ordering::Relaxed),
             axis_draws: ax.axis_draws.load(Ordering::Relaxed),
@@ -4831,7 +4850,10 @@ impl ArmSelectorPooledStats {
 /// are read on the coin-drawn runs and train nothing. `explore` is the
 /// exploration draw that splits the assigned runs into coin-drawn runs and
 /// learner runs. `observations` counts every run the selector observed,
-/// coin-drawn or learner run; `reward_runs_by_arm` and
+/// coin-drawn or learner run, and `cross_credited_runs` the credits of
+/// learner runs into the two learners that did not draw them, so every
+/// learner's `observations` is the run count plus one third of this;
+/// `reward_runs_by_arm` and
 /// `reward_positive_by_arm` split the first learner's reward over those
 /// runs by campaign arm, slot `arm_index + 1`, so a young arm's rate can be
 /// read against a mature one. `pooled` is the traffic on the cells shared
@@ -4843,6 +4865,7 @@ pub struct ArmSelectorAxisStats {
     pub axis_leader_agreements: u64,
     pub axis_draws: u64,
     pub observations: u64,
+    pub cross_credited_runs: u64,
     pub reward_runs_treated: u64,
     pub reward_positive_treated: u64,
     pub reward_runs_control: u64,
@@ -4881,6 +4904,7 @@ impl ArmSelectorAxisStats {
             axis_leader_agreements: a.axis_leader_agreements,
             axis_draws: a.axis_draws,
             observations: AX_OBSERVATIONS.load(Ordering::Relaxed),
+            cross_credited_runs: AX_CROSS_CREDITS.load(Ordering::Relaxed),
             reward_runs_treated: a.reward_runs_treated,
             reward_positive_treated: a.reward_positive_treated,
             reward_runs_control: a.reward_runs_control,
diff --git a/spur-core/tests/util_stats_export_completeness.rs b/spur-core/tests/util_stats_export_completeness.rs
index 9fb160d..97e8d8a 100644
--- a/spur-core/tests/util_stats_export_completeness.rs
+++ b/spur-core/tests/util_stats_export_completeness.rs
@@ -810,6 +810,7 @@ fn arm_selector_reward(m: &mut Marks) -> ArmSelectorRewardStats {
 fn arm_selector_learner(m: &mut Marks) -> ArmSelectorLearnerStats {
     ArmSelectorLearnerStats {
         chosen_runs: m.int(),
+        observations: m.int(),
         departures: m.int(),
         axis_leader_agreements: m.int(),
         axis_draws: m.int(),
@@ -864,6 +865,7 @@ fn arm_selector_axis(m: &mut Marks) -> ArmSelectorAxisStats {
         axis_leader_agreements: m.int(),
         axis_draws: m.int(),
         observations: m.int(),
+        cross_credited_runs: m.int(),
         reward_runs_treated: m.int(),
         reward_positive_treated: m.int(),
         reward_runs_control: m.int(),
@@ -940,6 +942,7 @@ fn arm_selector_reward_leaves(prefix: &str, r: &ArmSelectorRewardStats) -> Vec<(
 fn arm_selector_learner_leaves(prefix: &str, l: &ArmSelectorLearnerStats) -> Vec<(String, Value)> {
     let ArmSelectorLearnerStats {
         chosen_runs,
+        observations,
         departures,
         axis_leader_agreements,
         axis_draws,
@@ -961,6 +964,7 @@ fn arm_selector_learner_leaves(prefix: &str, l: &ArmSelectorLearnerStats) -> Vec
     } = l;
     let mut out = vec![
         leaf(prefix, "chosen_runs", *chosen_runs),
+        leaf(prefix, "observations", *observations),
         leaf(prefix, "departures", *departures),
         leaf(prefix, "axis_leader_agreements", *axis_leader_agreements),
         leaf(prefix, "axis_draws", *axis_draws),
@@ -1047,6 +1051,7 @@ fn arm_selector_axis_leaves(prefix: &str, a: &ArmSelectorAxisStats) -> Vec<(Stri
         axis_leader_agreements,
         axis_draws,
         observations,
+        cross_credited_runs,
         reward_runs_treated,
         reward_positive_treated,
         reward_runs_control,
@@ -1080,6 +1085,7 @@ fn arm_selector_axis_leaves(prefix: &str, a: &ArmSelectorAxisStats) -> Vec<(Stri
         leaf(prefix, "axis_leader_agreements", *axis_leader_agreements),
         leaf(prefix, "axis_draws", *axis_draws),
         leaf(prefix, "observations", *observations),
+        leaf(prefix, "cross_credited_runs", *cross_credited_runs),
         leaf(prefix, "reward_runs_treated", *reward_runs_treated),
         leaf(prefix, "reward_positive_treated", *reward_positive_treated),
         leaf(prefix, "reward_runs_control", *reward_runs_control),
