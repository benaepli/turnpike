# Replay frontier implementation review

Verdict: no correctness blocker or frozen-definition deviation found in the reviewed dispatch, replay-boundary, event-identity, corpus-service, and ordinary-history paths. This is a read-only source review against Spur baseline b86baad3f94ce14dbdf20688b61fd92317a75cc2 and the first hypothesis in research/lite/plans/iteration-69-admitted.json. No builds or experiments were run by this reviewer. The inspected working tree includes the bounded per-parent applied-offer bitset correction.

The strongest remaining case against the mechanism is scientific: later raw tape boundaries need not reconstruct the parent decision, broad same-node dependencies can be inert, and scanning legal identities plus longer tapes can overwhelm any search gain. Source inspection does not resolve those frozen applicability, utility, or cost predictions.

## Checks

- `spur-core/src/simulator/core/scheduler.rs:1235` captures both effective raw-word position and consumed replay-source position before route/queue/within-queue selection. The ordinary selection runs once before frontier substitution. `frontier_choice` at line 871 uses those saved pre-draw positions, accepts only the exact exhausted requested prefix boundary, and marks a request attempted once. Passed boundaries, missing keys, ambiguous keys, ineligible keys, and already-selected alternatives leave the stock choice in place. The child stock winner may differ from the parent winner only with an explicit mismatch diagnostic, as frozen.
- `scheduler.rs:788` keys records by node role/index, origin role/index/incarnation, send ordinal, pc/entry_pc, and link tag/sequence. Fault keys include planned node, current incarnation, and the pure retarget victim and its incarnation. Channel sends and timers have no frontier key. Lines 910-940 reject nonunique matches across both local and network queues; no saved vector index survives queue changes. The dependency predicate permits two remote entries at one receiver or the declared same-node fault/record race.
- `scheduler.rs:1105` separates side-effect-free eligibility from existing victim-hold counting. The frontier uses the reservation, FIFO, crash-hold and already-crashed masks without recounting their observations. Timer alternatives are excluded. Lines 883-887 require a candidate network event to remain itself under the complete fresh-first/pair-order preview.
- `scheduler.rs:1400-1451` preserves one real dispatch commit. A fallback or no-op commits the original draw through its ordinary preference chain. An applied alternative commits its already-validated slot. Real preference bookkeeping, rush observation, first-delivery accounting, queue removal, and execution occur once. Pure previews at lines 1953 and 2173 do not mutate displacement state or emit preference counters. Inspection of the baseline fresh-first and pair-order implementations found equivalent behavior after this factoring.
- `scheduler.rs:767` uses the same absorber decision as ordinary retargeting. Removing a selected planned crash changes only that planned node's pending-crash ledger; absorber ranking treats the planned node specially before the pending-pair filter, so this does not change the retarget result. Ordinary `retarget_crash` remains the one counted application at line 1470.
- `replay_corpus.rs:121-198` retains at most eight offers, truncates the parent tape to the largest retained request boundary with the original signal cut as a lower bound, and gives children only their selected prefix. A separate prefix-child cursor visits distinct retained offers before repeating while all siblings consume the unchanged eight-child budget. No-offer prefix children use the original signal cut; plan-only children receive no branch or replay tape.
- `replay_frontier.rs:179-223` keeps at most eight in-progress parent windows, each with eight cumulative write-counter samples per node and explicit first-touch order. The observer handles write-counter resets with signed deltas and later-created client identities. `diagnostic` at line 288 freezes the earliest parent-touched common node with NodeId tie-breaks and marks divergence only for complete eight-step windows. `path.rs:774` observes after ordinary scheduling, without changing the client history or termination path.
- `util_stats.rs:5073-5118` uses the shared bounded offer-index bitset to count repeated applied siblings even when diagnostic rows are dropped. All applied runs remain in applicability and divergence denominators; incomplete or missing-common-node windows are counted as censored. The parent diagnostic table and each parent's child list are bounded independently of these aggregate counters.
- `explorer.rs:1139-1267`, `campaign.rs:447-552`, and the narrow `path.rs` diff add request/capture state and reporting while retaining independent ordinary runs, their own selected arms and caps, stock feedback/credit rules, complete call/response histories, and baseline logging. No state checkpoint, synthesized continuation, added simulated step, or alternate linearizability recording path was introduced.

The root review owns final storage accounting and validation evidence. This review does not certify effect sizes, throughput, applicability floors, or unrun checks.
