# Independent pre-grade review

Verdict: PASS for the reviewed scope. No concrete blocking semantic or frozen-definition error found. This is permission to proceed to the ordinary first grade on implementation grounds only, not evidence of supply, utility, persistence growth, timer exposure, or throughput.

Reviewed the lite skill, the full selected hypothesis and required constraints in iteration-71-admitted.json, implementation.json, ownership-layout.md, current isolated source, baseline source, and retained test logs. Source references below are relative to tmp/loop/wt-lite-chain-demotion/spur/spur-core/src/simulator/ unless otherwise stated. No subject, configuration, protected file, or experiment was modified or run. Only this report was written.

## Mechanism and definitions

- `replay_chain.rs:182` gates activation on actual prefix-child context, raw source exhaustion and the child's own signal. The corrected minimum is three remaining total scheduling steps. Sampling at activation uses rejection sampling on a private hash stream and picks two distinct future points in current+1 through cap-1. Neither draw consults a simulator RNG. Demotion retains only two IDs; the second distinct demotion ranks below the first and repeats preserve ordering. The most recently executed chain is retained across faults, while faults break histogram stretches. These are separate required meanings.
- `campaign.rs:489` sets the child flag only in the actual `GridRun::Child` branch. `explorer.rs:1076` additionally requires the existing prefix bit and supplied tape. Fresh unfilled slots and AOS cannot activate. Child admission, parent source tape/cut, workload, plan-only LiveRng branch and corpus service remain baseline behavior in the reviewed diff.
- `rng.rs:299` checks source cursor against source length, independently of the produced recording length. Fallback recording continues after exhaustion. `path.rs:453,544,921` freezes the effective cap and passes the child signal/exhaustion at step boundaries. The exhaustion delay is observed at the end of the consuming step, so a following-boundary activation reports that boundary delay.
- `core/scheduler.rs:607-698` keeps the baseline tournament's sampled sequence, stock score comparison and first-sampled tie behavior. Chain ranking is computed over executable members of that same sequence. A non-executable stock winner is retained. Proportional selection and singleton decisions return the stock pick and have separate exclusion counters. The ranking does not alter queue selection or eligibility.
- `core/scheduler.rs:1177-1186,1730-1937` previews stock fresh-first and pair-order using immutable state and suppresses shadow counters. The actual fresh-first path performs displacement/take bookkeeping once; pair-order performs only actual counters once. Comparison after preferences uses the final stock preview versus the actual final choice. Comparison before preferences uses the sampled stock choice. The baseline fresh-first implementation was compared directly with its extracted preview/commit equivalent.
- `eligible_tournaments` includes all active non-singleton tournament decisions; `multi_chain_samples` includes those containing at least two distinct executable chain IDs, irrespective of the stock winner. `authority_decisions` additionally requires an executable stock winner. The final 15% numerator increments only within that authority population, including the local/network/timer selection branches. The zero/one/two_or_more sample histogram computes the exact frozen >=2 distinction without retaining a dynamic sample list.
- `replay_chain.rs:256-323` qualifies the prefix and plan-only histograms using at least eight selected suffix dispatches, not suffix scheduling steps. `active_eight_steps` remains separate. Plan-only observation starts after that child's first signal; prefix observation starts at activation. Different chains, faults/timers, blocked executable dispatches, and steps with no selected executable end stretches. Final censored stretches enter the main length histogram once and are also exported separately. Dispatch/timer exposure uses the same qualified population. Arm and readiness strata are exported, with readiness recorded at the first dispatch of each stretch. These are descriptive selected-population diagnostics, as frozen.

## Storage and artifact identity

The baseline nested HEAD independently resolves to b86baad3f94ce14dbdf20688b61fd92317a75cc2. Recomputed source-tree hash equals implementation.json: b6f15bedd9fdd59aef99ec70c6c181ab126b767fbc04b04748d87063193738f3. Exported spur.patch and general_vr.json hashes also match their recorded hashes.

Compared `core/state.rs:2202` untagged layout mirrors against the actual baseline Record/Runnable definitions and baseline partition QueuedMessage definition. Field types, order and enum variants match the baseline, including receiver_token_at_send and all ordinary fault variants. These are current source mirrors, not a baseline-cache memory metric. Both hash-policy measurements in focused-tests.log report Record 240->248, Runnable 240->248, QueuedMessage 256->264. Their compiled test is present and passes in the final full test log. RunAttribution mirrors in layout-scratch.rs also match the source fields and report no growth.

`ChainState` contains fixed scalars, two points, two demotion IDs, 128 u32 length bins and fixed exposure arrays; it has no owned heap allocation. Retained state is 688 bytes. Fixed selector scratch is also represented accurately by layout-scratch.rs, including the optional ranked tuple and shadow index. The reported scratch values plus the retained state remain below 1024 bytes; ordinary executable metadata stays one u64 per object. No new per-chain map, per-step log or ancestry graph was found in this scope. Global aggregate tables have fixed dimensions and contain no individual run objects. No RSS conclusion follows from these checks.

## Validation and limits

Independently summed tests.log: 471 passed, zero failed across 25 suites. Read the actual tests for sampled-winner/RNG parity, mixed fault preservation, same-chain/proportional/singleton exclusions, queue-class empty fallbacks, pure shadow plus single displacement commit, source exhaustion, three-step minimum, repeat/no-chain points, histogram qualification/censoring/reset, and export render/delta/accumulation. Lifecycle tests exercise timer and channel wakeups plus crash/recovery identity. Existing test success is retained evidence; this review did not rerun tests or a simulator.

The sole firing smoke predates the final explicit sampled-chain-count histogram addition; implementation.json distinguishes its binary/source identity. Its results establish firing only. The final tree hash and full test log include the added histogram.

Root is separately reviewing full propagation, execution, path semantics and all remaining diff; this review is not a substitute for that coverage. Tournament RNG tests compare stock versus active decisions on the same state rather than claiming equal whole-run RNG use after schedules diverge. Layout mirrors substantiate current type growth but are not a total-process peak measurement. Length bucket 32 is >=32, so a median in that bucket is bounded, not an exact value of 32; the grader must not claim a precise 25% increase from an unresolved top-bucket comparison. No applicability, efficacy or baseline performance measurement was performed. Raw prefix/plan-only 1.20-1.60 and paired baseline-adjusted 1.15-1.50 remain distinct frozen bands.
