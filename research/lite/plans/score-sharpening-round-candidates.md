# Candidates for judging, round 7 - sharpening the SCORE (measurement, not scheduler)

The user lifted protection on research/oracle and traceanalyzer for PROPOSAL only. Nothing is built this round.

## 1. stale-incarnation-deliver (self 8)
Type the deliver label by sender incarnation. Oracle: add `"sender_incarnation": "dead_restarted"` to `deliver_svc_1_to_2` and add dependency [recover_nl, deliver_svc_1_to_2]. Claims serde on the Rust side ignores the extra key (plan_config.rs EventSpec::Deliver has no deny_unknown_fields) so tiers/ plans stay runnable. Matcher: planconfig.go EventSpec gains SenderIncarnation; candidates.go collectDelivers admits an Enter row only if the sender's Crash (c) and Recover (r) rows bracket the message: dispatch_step < c < r < enter_step ("crossing" = crash only). Reader: EnterRow gains DispatchStep via arg_min(step, seq_num) in the existing Dispatch CTE. Grader: PRIMARY_RUNG (decide.ts:284) moves 6 -> 8 because depth>=6 still has routes avoiding the typed label while every depth>=8 chain uses it.
PREDICATE over incarnation/step/node only: Dispatch on s at d0, Enter on receiver at e, Crash(s) at c with d0<c<e, Recover(s) at r with c<r<e. No payload. This is grade.go's h2 localised to one message.
MEANING: buys bug.md step 3 exactly (node 1 receives the message NL sent BEFORE crashing AFTER NL is back). Buys nothing else - wants pairing with #2.
POWER (measured by the proposer on a 120s current-tree explore, 250,293 runs, parquet queried directly): strict ordered prefix write->timer->crash_1->SVC 1->2 after crash: 1,104 of 29,999 runs; adding dead_restarted: 821 (1.34x cost). Real grader on 12,000 runs: 100 of 105 depth>=8 chains contain deliver_svc_1_to_2, so depth>=8 falls from ~4,010/chunk to ~3,000/chunk (~10/s) - 3x above the ~1,000 floor; SE ~3.7%/chunk. depth>=6 moves ~-5%, hence primary -> 8.
VALIDATION: (1) regenerate findbug_archive (gitignored; manifest records generator commit b04c56f0 + sha256s) and require top-rung precision >= 71% and all 266 violations still at max depth; precision should RISE since the 106 deep-but-linearizable runs are those whose view-change message was not stale. (2) nonce-bug archive must NOT populate the top rung. (3) baseline chunks: depth>=8 shrinks ~25%, not ~0% (predicate never fires) nor ~100% (join wrong).
GOODHART: inflatable by making crash-with-sends-in-flight more common (iterations 9,10,13,14 moved that at will); hard to game because anchored to a NAMED handler at a NAMED chain position - iteration 13 raised stale deliveries 47% and depth fell 21%.
MIGRATION: epoch bump per the allow_timer precedent; manifest invariants 2,3 recomputed; both baseline caches re-measured (template sha changes); PRIMARY_RUNG changes; graderVersion bump so pre/post chunks cannot pool.

## 2. witness-complete-symmetric-depth (self 9)
Oracle: one top-level key `"node_symmetry": true` in relax_minimal_general.json only (NOT tiers/, which the Rust side executes as plans and validates top-level keys). Events/deps unchanged; node indices become role placeholders.
Matcher: (a) rootAnchoredPrefix currently walks a.edges, the TRANSITIVE CLOSURE, so a chain may skip any label. Replace with a witness-complete recursion over the DIRECT edge set a.depIn: dp[v] = 1 + max over direct predecessors only, v eligible only if EVERY matchable direct predecessor is matched and strictly earlier (zero-candidate predecessors contracted as today). assignEarliestAfterPredecessors tightened likewise. (b) under node_symmetry, run matching once per bijection sigma on server indices (fixed per run), apply sigma to spec.Target/From/To in buildCandidates, take max depth; prune permutations whose rung-3 labels have no candidates (~2/3 die immediately).
Grader: PRIMARY_RUNG becomes 6 in the new numbering (write, timer, crash of B, delivery B->C, crash of C, recovery of C); rungs 7-9 diagnostics only. Grade wall budget must rise (ComputeDagOrderOpts ~94s of 300s today; sharing the reader pass + pruning keeps it to 2-3x).
PREDICATES over step order and node identity only: witness-completeness (every matchable direct predecessor matched strictly earlier); node symmetry (max over bijections of server indices, one bijection per run).
MEANING - THE HEADLINE CLAIM: grading 12,000 runs of a current-tree store with the real grader: depth_at_least = [12000, 11545, 10347, 8980, 5929, 2884, 826, 105, 3] (depth>=6 at 24.0%; cached baseline reads 22.8%). Of the 2,884 depth>=6 runs, 1,961 (68%) have NO crash_nl anywhere on the winning chain; 136 have no crash label of any kind; only 111 (3.8%) carry all six core labels. At depth>=8, 78 of 105 have no crash_nl. Cause: rootAnchoredPrefix walks the transitive closure, so 'w1 -> allow_t1 -> deliver_svc_1_to_2 -> crash_2 -> recover_2 -> deliver_svc_1_to_0' is a legal depth-6 chain that never crashed the node whose message is supposed to go stale. THE GRADED PRIMARY RUNG TODAY DOES NOT REQUIRE THE BUG'S INITIATING FAULT. After: depth>=k means the first k events of bug.md happened in order with nothing skipped.
POWER (measured, same store, strict ordered chain in SQL, greedy earliest witness): fixed identities rung3 2,185; r4 1,104; r5 96; r6 90; r7 15; r8 5; r9 0. Union over 6 bijections: r3 9,735; r4 5,025; r5 574; r6 537; r7 62; r8 27; r9 2 (symmetry buys 4.5-6x). Projected per 538,980-run chunk: r4 66,000; r5 8,070; r6 7,730; r7 755; r8 270; r9 36. Rung 6 sits 7.7x above the floor -> primary; 7+ below floor -> diagnostics. Without symmetry r6 = 1,294/chunk, at the floor - the two changes belong together. Half-split log-ratio SE at r6 ~2.3%/chunk vs 0.57% today.
VALIDATION: (1) regenerated findbug_archive: new top-rung precision must EXCEED 71%; the plan forces exactly this witness-complete order so the plan corpus barely loses depth while the general baseline loses ~16x - that ratio measures what the score was missing. (2) tiers/ monotonicity: find_bug_plan should now dominate relax_5 and relax_3 in mean depth (manifest records it does NOT today). (3) nonce-bug archive must not populate the top rung. (4) unconstrained_c0 (max depth 4) must not gain depth. (5) permutation-independence check.
GOODHART: crash spam raises r3/5/6 - defend by reporting the conditional advance r6/r3 (537/9,735 = 5.5%), which crash spam does not move; witness-completeness REMOVES today's cheap inflation route (lengthen runs so unrelated deliveries/reads accumulate); symmetry cannot be gamed (group action, no new event). Residual: rung 6 stops at bug.md step 4.
MIGRATION: heavier - the depth SCALE changes (primary per-run probability ~23% -> ~1.4%); both baseline caches, all corpora + manifest invariants 1-3, PRIMARY_RUNG + rung list, nullBand/MERGE_Z calibration (4x relative noise), grade wall budget; graderVersion bump; historical depth figures annotated by epoch.

## 3. commit-time-client-labels (self 5)
Oracle: optional `"at": "response"` on write/read specs; set on w2, r1-r3; add deliver_sv_to_0 / deliver_sv_to_2 (StartView, present in tiers/find_bug_plan.json) back with edges w2 -> deliver_sv_to_x. Matcher: EventSpec.At; collectClientInvocations matches the Response row when At == response, via a per-run unique_id -> (target,key) map from Invocation rows (unique_id is on both rows; path.rs:638). Grader: no change to the primary - diagnostic only.
PREDICATE: an acknowledged client operation completed before a named message was delivered, over operation identity and step order.
MEANING: makes bug.md step 7 checkable (a write acknowledged on a replica that had not yet learned of the new view). The only one of the three unexpressible conditions reachable without payload semantics; "recovered in view 0" (step 2) is a payload statement and treated as disqualifying.
POWER: weak, stated plainly - already 755/chunk at the second-write rung, 270 at the SVC-to-A rung under #2; requiring Response and preceding the StartView delivery cuts further. Far below the floor; cannot gate. Value: a 372-run classification instrument on the plan corpus (266 violations vs 106 deep-but-linearizable). Also: the general config issues 2-4 writes over one key with uniform target, so a SECOND write to the chain's node exists in few runs - raising num_write_ops is a cheaper way to buy this rung than any mechanism.
VALIDATION: on the 372 full-depth corpus runs, the rung must fire on substantially more of the 266 violations than the 106 linearizable; near-silent on the nonce archive; small but nonzero on baseline chunks.
GOODHART: low as a diagnostic; if gated, delaying the last hop of a view change while client traffic flows widens the window mechanically, and it is gameable through the workload config.
MIGRATION: adds vertices/edges so max depth changes; same discipline; free if landed with #2.

## 4. randomized-within-session-primary (self 8)
No oracle/matcher change. Grader: the gate's primary becomes the probe-free variantContrast ratio at the primary rung (decide.ts:115 already computes it with the iteration-8 probe fix), tested against 1.0 with its log-ratio interval. Cross-binary per-second stays computed and stays a BLOCKER on regression (the only honest read of shared hot-path cost, iteration 14) but stops being what a merge is granted on. Two additions: (a) declared-treatment requirement - a candidate must name its randomizing variant bit, else the internal primary does not apply; (b) an automatic build-layout null (the bb-control protocol) once per session, as the null band for candidates with no internal control (config-only ablations like iteration 11; whole-binary changes like iteration 14) which fall back to cross-binary with a ~5% floor.
MEANING: changes WHAT IS COMPARED, not what is measured. Evidence: bb-build-layout-control (identical source, two builds: throughput 0.9634, depth>=6/s 0.9510 vs 0.0045 null); iteration 11's A/A same-binary null ~2% (4x the band); iterations 8 and 10 cross-binary reads disagreed in SIGN with their own internal control; iteration 16 moved per-run depth 1.0500 [1.0448,1.0551] - nine SE above 1.0 - and could not be separated cross-binary (1.0301 vs 0.0023 null).
POWER: improves - internal half-split SE at depth>=6 ~0.57%/chunk vs a real cross-binary floor of 4-5%: ~8x gain in resolvable effect. Paired with #2's rung 6: SE ~2.3%/chunk, 1.6% over two - still better than the cross-binary floor. Limitation written into the rule: the internal contrast is blind to a cost every run pays (iteration 14's 23% throughput loss), hence cross-binary throughput stays a blocking cost check.
VALIDATION: (1) paper replay against the record - the rule must merge crash placement, the flip's two steps, the ablation; close iterations 9 (0.9948 vs cross 1.0156), 10 (0.9855 vs 1.0407), 13 (0.7914), 14 (0.9646); MERGE iteration 16 (1.0500 [1.0448,1.0551]). All numbers in research/lite/observations.md; costs no chunks. (2) A/A with a synthetic randomizing bit must read 1.000 within interval. (3) the run-cap-probe bit reads ~1.0 when treated does not exempt probes (iteration 6: 0.976 [0.940,1.014]).
GOODHART: a mechanism can choose its own treatment population and thereby its control (iteration 8: exempting probes made the contrast read 1.047 vs 0.985 probe-free). Rule: the declared bit must be a blind coin on the run id, uncorrelated with other learners' phases; refuse a bit whose control differs from treated in steps/run or arm composition beyond tolerance. Second: without a cross-binary gate a 20%-slower binary that helps treated runs 5% looks like a win - hence throughput stays a hard blocker.
MIGRATION: lightest - no epoch bump; decide.ts ruleVerdict, stopper.ts stopping criterion, grader.ts reports, the frozen-prediction template (bands move to per-run terms), and the filed crash-fanout candidate becomes immediately decidable. Baselines and corpora untouched. Constraint: config-only ablations and whole-binary refactors have no bit and keep the cross-binary path with the 5% floor.
